/*
  ES6+ pra frente é considerado javascript moderno
  Os browsers acompanham de forma mais lenta para 
  evitar a quebra das páginas

  É possível utilizar o ES6+ usando os transpiladores que
  convertem o javascript moderno para linguagem compativel
  com o brwoser.

  ATENÇÃO:>>>>> TypeScript é um TRANSPILADOR

  var - para declarar variável e tem escopo amplo
  let - para declarar variável e tem escopo reduzido
  const - para declarar variável e não é possível reatribuir algo
  const não se aplica a vetor e a objeto

  Arrow function - possui sintaxe mais simples que a function
  Ao inves de escrever function vc coloca o parametro e =>

  Template literal - concatena strings utilizando o ${} para passar o valor
*/
