/* Métodos de array
  map -> para gerar novo array transformando os dados
  filter -> gera um novo array filtrando com uma condição definida 
  forEach -> percorre todos os elementos do array
  reduce -> geralmente usado para somar, realiza calculo iterativo
            com base no elementos
  find -> encontra elementos com base na lógica definida
  some -> verifica se há um elemento que atende a preposição
  sort -> ordena os elementos com base no critério definido
*/
